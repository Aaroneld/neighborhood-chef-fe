import makeStyles from '@material-ui/styles/makeStyles';

export const styles = makeStyles({
  container: {
    '& .MuiTextField-root': {
      width: '98%',
    },

    '& label': {
      color: 'rgba(0,0,0,.6)',
    },
  },
});
